[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "review-project-test"
version = "0.1.0"
description = "Synthetic fixtures for validating an AI code review agent."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
