# Synthetic validation repository bootstrap

## Purpose

Initialise an isolated public repository for live GitHub PR validation of ByteCodeReviewAgent.
The repository must contain only synthetic code and fictional data.

## Baseline

- Added a small Python order-service package.
- Added passing unit tests for total and average calculations.
- Added repository guardrails forbidding real credentials and production code.

## Verification

- Baseline unit tests must pass before the validation branch is created.

## Next step

Create a `codex/` branch that introduces reviewable defects, push both branches, open a pull
request, and run ByteCodeReviewAgent against the PR URL.
